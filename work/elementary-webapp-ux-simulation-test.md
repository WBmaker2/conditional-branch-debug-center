# 인터랙티브 시뮬레이션 검증

## 판정

`elementary-webapp-ux-simulation-decision.md`에서 `not-needed`로 결정했다. 이번 변경에는 새 시뮬레이션 모델·렌더러·변수·seed·clock·pause·step이 없으므로 시뮬레이션 전용 검증 항목은 N/A이다.

## 대신 검증하는 기존 학습 루프

- 예측 라디오 선택 → 규칙을 한 줄씩 시험 → 규칙별 근거 재표시 → 갭/겹침/한 규칙 선택 → 최소 수리 → finiteDomain 전체 재시험
- 동일 미션·동일 입력에 대해 evaluator/reducer의 판정 계약과 기존 단위·E2E 테스트를 유지
- 320/375/1280px에서 DOM 텍스트와 CTA가 보이며, 축소 모션에서는 기존 정적 `필수` 배지와 외곽선을 유지

## N/A 사유

시간 기반 자동 실행이 아니므로 pause/step이 필요하지 않다. 시뮬레이션의 수치·정답·측정은 생성 이미지에 의존하지 않으며, 모든 규칙 결과는 검수된 데이터와 순수 evaluator에서 나온다.
